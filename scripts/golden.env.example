# Example environment for golden workflows
GTFS_VALIDATOR_BIN=
GOLDEN_MANIFEST=scripts/golden_manifest.tsv
GOLDEN_ACTUAL_DIR=golden_actual
COMPARE_FLAGS="--strip-runtime-fields --skip-html --ignore-notice-order --ignore-input --sort-summary-arrays --ignore-validator-version"
COMPARE_EXTRA_JSON="notice_schema.json"
COMPARE_HTML_NAME=report.html
# Set to custom HTML report filename if needed.
MANIFEST_VALIDATE_FLAGS="--warn-empty-expected"
# Filter cases by glob pattern (e.g., "*feed-a*")
CASE_FILTER=
# UPDATE_EXPECTED_ON_FAIL=1
# UPDATE_EXPECTED=1
# UPDATE_EXPECTED_FLAGS="--overwrite"
# SKIP_MANIFEST_VALIDATE=1
