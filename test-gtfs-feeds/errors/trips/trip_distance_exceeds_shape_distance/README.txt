The distance between the last shape point and last stop is >= 11.1m threshold.

This test has stops that extend beyond the shape_dist_traveled value,
indicating the trip distance exceeds the shape distance.

Expected notice: trip_distance_exceeds_shape_distance
